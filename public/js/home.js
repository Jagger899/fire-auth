const mailContainer = document.querySelector('.mail-container');
const shownContainer = 'container mail-container shown-container';
const hiddenMailContainer = "container mail-container hidden-container";